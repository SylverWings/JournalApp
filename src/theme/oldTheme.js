import { createTheme } from "@mui/material";


export const oldTheme = createTheme({

    palette:{
        primary:{
            main: '#663702'
        },
        error: {
            main: '#DAC414'
        }
    }
})